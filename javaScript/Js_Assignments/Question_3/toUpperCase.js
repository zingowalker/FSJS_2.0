// 03. Declare a variable and assign string value to 
// it and change all the string characters to 
// capital letters using toUpperCase() method.

const text = "the quick brown fox jump over the lazy dog";

const toUpperCase = text.toUpperCase()

console.log(toUpperCase)