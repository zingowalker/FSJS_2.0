// 08. Demonstrate the use of trim() to remove any 
// trailing whitespace at the beginning and 
// the end of a string.

const whitespace = '  remove whitespace  '
console.log(whitespace)
console.log(whitespace.trim()) // trim method removes white space from front and the back 